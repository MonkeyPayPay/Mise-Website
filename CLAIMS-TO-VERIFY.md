# CLAIMS-TO-VERIFY — product claims not (yet) publishable as stated

Per the build brief: every product claim on the site must be sourced from the platform's
design docs / code. Claims below could **not** be fully verified against the platform repo
(`MonkeyPayPay/Reservation-Scheduling-App`, branch `claude/demo-mode-mise-restaurante-hc64pr`,
audited 2026-08-01) and were either **softened on the site** or **left off** pending sign-off.

| # | Claim (from the brief) | What the code/docs actually show | What the site says instead | To publish the strong claim |
|---|---|---|---|---|
| 1 | **"EMV/tap payments"** on the POS | **Zero hits** for EMV / tap-to-pay in server, shared or client code. Payments run through the Stripe adapter (tokenized cards, deposits, card-on-file); no card-present/terminal-hardware integration found. | "Card payments, deposits and card-on-file run through Stripe — tokens, never card numbers." No EMV/tap wording anywhere. | Ship a card-present integration (e.g. Stripe Terminal) or confirm it exists somewhere I couldn't see. |
| 2 | **Native iOS & Android apps available** | Capacitor wrapping + CI jobs exist (`.github/workflows/build-apps.yml` per DEMO-URLS.md), but **no signed builds, no store listings**. TestFlight/Play links are `[FILL]` in DEMO-URLS.md. | "In-browser preview is live now · native store builds rolling out." Store buttons render **disabled** (TODO-DEMO-URL placeholders in `assets/demo-urls.js`). | Publish the TestFlight/Play links → paste them into `assets/demo-urls.js` (`ios`, `android`). |
| 3 | **"Offline-first" POS** | Verified: offline outbox with idempotent replay (`pos.routes.ts` — clientRef resolution, outage reporting). But "offline-FIRST" (full offline boot/local-first store) is a stronger architecture claim than an offline queue. | "Offline-resilient by design — terminals queue every action and replay idempotently." | Confirm the terminal boots and rings fully offline, or keep "offline-resilient". |
| 4 | **Pricing figures** | §5 of DESIGN-PRICING-PACKAGING is explicit: numbers are **launch defaults, price-book data, editable in the admin**. The billing engine itself is design-only (ships no code yet). | Pricing page renders from `assets/pricing-model.js` mirroring the doc, labeled "launch defaults"; builder notes "your order confirms the exact figure". | Owner sign-off on final launch prices; then just edit `assets/pricing-model.js` (site) and the platform price book together. |
| 5 | **14-day trial, no card** | In DESIGN-PRICING-PACKAGING §6.3 (design), not yet running code — billing/trials are unbuilt. | Mentioned once, softly, on the pricing page ("when you're ready — no card to start"). | Ship the trial flow (platform PR track), or remove until then. |
| 6 | **Reservation channel connections (Yelp / OpenTable)** | Verified in code (`server/src/channels/yelp.ts`, `opentable.ts`, runner, types) behind the `reservation-channels` flag. **But** live operation needs channel partner agreements (noted in pricing design §4.1). | Listed as a capability with tier gating shown honestly on /pricing.html. | Confirm partner/API agreements exist before implying live two-way OpenTable/Yelp sync in production. |
| 7 | **Demo URLs live in production DNS** | DEMO-URLS.md declares the five `demo*.mise-hospitality.com` hosts final, but DNS + the demo deployment may not be stood up yet (deployment crib is in the doc; I cannot reach the network to probe). | Links wired to the final URLs via `assets/demo-urls.js` (single swap point). | Stand up the demo node + wildcard DNS/cert per DEMO-URLS.md "Deployment"; then links just work. |
| 8 | **Social proof** | No customers yet (D1 on the roadmap). | Homepage has an honest placeholder section (`<!-- TODO-SOCIAL-PROOF -->`), no invented logos/quotes/stats. | Real customer names/numbers with permission. |
| 9 | **"Drawer management" depth** | Verified: drawer sessions/cash handling in POS code (13 files). Blind counts/over-short specifics appear in code; phrasing kept to what's there. | "Drawer sessions, paid-ins/outs, blind counts and over/short land in the cash ledger." | — (verified; listed for the record since wording is specific). |
| 10 | **Gift cards & house accounts (AR)** | Verified in code (11 + 7 files) and screenshot 28. Cross-location behavior tied to Business+ tiering per the gating matrix. | Described in the platform tour with tier note. | — (verified). |

**Process note:** anything added to the site later should follow the same rule — verify in
the platform repo first, else it goes in this file, not on a page.
